if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui", 10)
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")

if not PlayerGui then return end

if PlayerGui:FindFirstChild("GeminiAuraPremium") then
    PlayerGui.GeminiAuraPremium:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GeminiAuraPremium"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 999999

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 120, 0, 35)
ToggleBtn.Position = UDim2.new(0.05, 0, 0.1, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 182, 193)
ToggleBtn.TextColor3 = Color3.fromRGB(128, 0, 128)
ToggleBtn.Text = "🌸 Aura"
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 14
ToggleBtn.Parent = ScreenGui
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", ToggleBtn).Color = Color3.fromRGB(255, 105, 180)
ToggleBtn.UIStroke.Thickness = 2

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 340, 0, 260)
Frame.Position = UDim2.new(0.3, 0, 0.3, 0)
Frame.BackgroundColor3 = Color3.fromRGB(255, 240, 245)
Frame.Visible = true
Frame.Active = true
Frame.Parent = ScreenGui
Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 12)
Instance.new("UIStroke", Frame).Color = Color3.fromRGB(219, 112, 147)
Frame.UIStroke.Thickness = 2.5

local BgImage = Instance.new("ImageLabel")
BgImage.Size = UDim2.new(1, 0, 1, 0)
BgImage.BackgroundTransparency = 1
BgImage.Image = "rbxassetid://2949479029"
BgImage.ScaleType = Enum.ScaleType.Crop
BgImage.ImageTransparency = 0.85
BgImage.Parent = Frame

local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 38)
TopBar.BackgroundColor3 = Color3.fromRGB(255, 182, 193)
TopBar.Parent = Frame
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -20, 1, 0)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "✨ GEMINI AURA V4 ✨"
Title.TextColor3 = Color3.fromRGB(128, 0, 128)
Title.TextSize = 14
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = TopBar

local Glow = Instance.new("Frame")
Glow.Size = UDim2.new(1, 0, 0, 2)
Glow.Position = UDim2.new(0, 0, 1, -2)
Glow.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
Glow.BorderSizePixel = 0
Glow.Parent = TopBar

local dragging, dragInput, dragStart, startPos
local function update(input)
    local delta = input.Position - dragStart
    Frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end
Frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
Frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then update(input) end
end)

ToggleBtn.MouseButton1Click:Connect(function()
    Frame.Visible = not Frame.Visible
end)

local AuraBtn = Instance.new("TextButton")
AuraBtn.Size = UDim2.new(1, -30, 0, 45)
AuraBtn.Position = UDim2.new(0, 15, 0, 55)
AuraBtn.BackgroundColor3 = Color3.fromRGB(255, 228, 225)
AuraBtn.TextColor3 = Color3.fromRGB(200, 50, 70)
AuraBtn.Text = "KILL AURA : DISABLED"
AuraBtn.Font = Enum.Font.GothamBold
AuraBtn.TextSize = 13
AuraBtn.Parent = Frame
Instance.new("UICorner", AuraBtn).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", AuraBtn).Color = Color3.fromRGB(255, 105, 180)
AuraBtn.UIStroke.Thickness = 1.5

local LogLabel = Instance.new("TextLabel")
LogLabel.Size = UDim2.new(1, -30, 0, 110)
LogLabel.Position = UDim2.new(0, 15, 0, 110)
LogLabel.BackgroundTransparency = 1
LogLabel.TextColor3 = Color3.fromRGB(128, 0, 128)
LogLabel.TextSize = 12
LogLabel.Font = Enum.Font.Gotham
LogLabel.TextXAlignment = Enum.TextXAlignment.Left
LogLabel.TextYAlignment = Enum.TextYAlignment.Top
LogLabel.Text = "Log: waiting..."
LogLabel.TextWrapped = true
LogLabel.Parent = Frame

local killauraActive = false

AuraBtn.MouseButton1Click:Connect(function()
    killauraActive = not killauraActive
    if killauraActive then
        AuraBtn.BackgroundColor3 = Color3.fromRGB(230, 255, 230)
        AuraBtn.TextColor3 = Color3.fromRGB(50, 180, 80)
        AuraBtn.UIStroke.Color = Color3.fromRGB(152, 251, 152)
        AuraBtn.Text = "KILL AURA : ENABLED"
        LogLabel.Text = "Aura ON, searching..."
    else
        AuraBtn.BackgroundColor3 = Color3.fromRGB(255, 228, 225)
        AuraBtn.TextColor3 = Color3.fromRGB(200, 50, 70)
        AuraBtn.UIStroke.Color = Color3.fromRGB(255, 105, 180)
        AuraBtn.Text = "KILL AURA : DISABLED"
        LogLabel.Text = "Log: waiting..."
    end
end)

local function getKnife()
    if not LocalPlayer.Character then return nil end
    for _, item in pairs(LocalPlayer.Character:GetChildren()) do
        if item:IsA("Tool") then
            return item
        end
    end
    local bp = LocalPlayer:FindFirstChild("Backpack")
    if bp then
        for _, item in pairs(bp:GetChildren()) do
            if item:IsA("Tool") then
                return item
            end
        end
    end
    return nil
end

task.spawn(function()
    while task.wait(0.2) do
        if killauraActive then
            local zombies = {}
            local zombieNames = {}
            local logParts = {}

            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("Model") then
                    local hum = obj:FindFirstChild("Humanoid")
                    if hum and hum.Health > 0 then
                        if not Players:GetPlayerFromCharacter(obj) then
                            table.insert(zombies, obj)
                            if #zombieNames < 3 then
                                table.insert(zombieNames, obj.Name)
                            end
                        end
                    end
                end
            end

            if #zombies == 0 then
                LogLabel.Text = "No zombies found!"
                continue
            end

            table.insert(logParts, "Found: " .. #zombies .. " | " .. table.concat(zombieNames, ", "))

            local attackMethods = {
                method1 = function(zombie)
                    Lighting.ETC:FireServer({
                        zombie.Humanoid,
                        9e9,
                        "Damage\240\159\144\153",
                        false, false, false,
                        [8] = {x = 0, y = 0, z = 0},
                        [9] = { HitS = 1, ConfirmedHit = 1 }
                    })
                end,
                method2 = function(zombie)
                    pcall(function()
                        zombie.Humanoid:TakeDamage(9e9)
                    end)
                end,
                method3 = function(zombie)
                    local knife = getKnife()
                    if knife then
                        local handle = knife:FindFirstChild("Handle") or knife:FindFirstChildWhichIsA("BasePart")
                        if handle then
                            local hrp = zombie:FindFirstChild("HumanoidRootPart") or zombie:FindFirstChild("Torso") or zombie:FindFirstChild("Head")
                            if hrp then
                                firetouchinterest(handle, hrp, 0)
                                firetouchinterest(handle, hrp, 1)
                            end
                        end
                    end
                end,
            }

            local successCount = 0
            local usedMethod = ""

            for methodName, methodFunc in pairs(attackMethods) do
                local ok = true
                for _, zombie in pairs(zombies) do
                    local stat, err = pcall(methodFunc, zombie)
                    if not stat then
                        ok = false
                        break
                    end
                end
                if ok then
                    successCount = #zombies
                    usedMethod = methodName
                    break
                end
            end

            if successCount == 0 then
                table.insert(logParts, "All methods failed!")
            else
                table.insert(logParts, "Killed " .. successCount .. " via " .. usedMethod)
            end

            LogLabel.Text = table.concat(logParts, "\n")
        end
    end
end)
