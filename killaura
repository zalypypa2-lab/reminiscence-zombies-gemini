if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui", 10)
local UserInputService = game:GetService("UserInputService")

if not PlayerGui then return end

if PlayerGui:FindFirstChild("GeminiAuraPremium") then
    PlayerGui.GeminiAuraPremium:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GeminiAuraPremium"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 999999

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 110, 0, 35)
ToggleBtn.Position = UDim2.new(0.05, 0, 0.1, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
ToggleBtn.TextColor3 = Color3.fromRGB(180, 100, 255)
ToggleBtn.Text = "★ Gemini Aura"
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 14
ToggleBtn.Parent = ScreenGui

Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 6)
local toggleStroke = Instance.new("UIStroke", ToggleBtn)
toggleStroke.Color = Color3.fromRGB(140, 80, 220)
toggleStroke.Thickness = 1.5

-- ... остальное GUI без изменений ...

local AuraBtn = Instance.new("TextButton")
-- ... создание кнопки ауры ...

local killauraActive = false

AuraBtn.MouseButton1Click:Connect(function()
    killauraActive = not killauraActive
    if killauraActive then
        AuraBtn.BackgroundColor3 = Color3.fromRGB(15, 25, 15)
        AuraBtn.TextColor3 = Color3.fromRGB(70, 255, 70)
        AuraBtn:FindFirstChildOfClass("UIStroke").Color = Color3.fromRGB(30, 60, 30)
        AuraBtn.Text = "KILL AURA : ENABLED"
    else
        AuraBtn.BackgroundColor3 = Color3.fromRGB(20, 15, 15)
        AuraBtn.TextColor3 = Color3.fromRGB(255, 70, 70)
        AuraBtn:FindFirstChildOfClass("UIStroke").Color = Color3.fromRGB(60, 30, 30)
        AuraBtn.Text = "KILL AURA : DISABLED"
    end
end)

local function getWeapon()
    if not LocalPlayer.Character then return nil end
    for _, item in pairs(LocalPlayer.Character:GetChildren()) do
        if item:IsA("Tool") then
            return item
        end
    end
    -- если ничего не экипировано, проверяем рюкзак
    local bp = LocalPlayer:FindFirstChild("Backpack")
    if bp then
        for _, item in pairs(bp:GetChildren()) do
            if item:IsA("Tool") then
                return item
            end
        end
    end
    return nil
end

task.spawn(function()
    while task.wait(0.05) do
        if not killauraActive or not LocalPlayer.Character then continue end
        local myHrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local weapon = getWeapon()
        if not myHrp or not weapon then continue end
        
        local attackPart = weapon:FindFirstChild("Handle") or weapon:FindFirstChildWhichIsA("BasePart")
        if not attackPart then continue end

        pcall(function()
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("Model") and obj ~= LocalPlayer.Character then
                    local hum = obj:FindFirstChild("Humanoid")
                    local zHrp = obj:FindFirstChild("HumanoidRootPart")
                    if hum and zHrp and hum.Health > 0 and not Players:GetPlayerFromCharacter(obj) then
                        local dist = (myHrp.Position - zHrp.Position).Magnitude
                        if dist <= 22 then
                            firetouchinterest(attackPart, zHrp, 0)
                            firetouchinterest(attackPart, zHrp, 1)
                        end
                    end
                end
            end
        end)
    end
end)
